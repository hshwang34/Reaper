{
  "name": "reality-hijack",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "shared",
    "core",
    "sidecar",
    "web",
    "app",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n sidecar,web -c cyan,magenta \"npm:dev:sidecar\" \"npm:dev:web\"",
    "dev:sidecar": "npm run dev --workspace sidecar",
    "dev:web": "npm run dev --workspace web",
    "build": "npm run build --workspace web",
    "start": "npm run build && npm run start --workspace sidecar",
    "dev:app": "npm run build && npm run dev --workspace app",
    "typecheck": "npm run typecheck --workspace shared && npm run typecheck --workspace core && npm run typecheck --workspace sidecar && npm run typecheck --workspace web && npm run typecheck --workspace app && npm run typecheck --workspace server",
    "dev:server": "npm run dev --workspace server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  }
}
