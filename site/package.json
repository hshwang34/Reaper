{
  "name": "@rh/site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Marketing site for Reality Hijack. A standalone static build deployed to Vercel from this directory; deliberately NOT an npm workspace of the monorepo so hosting installs only Vite, never Electron or native modules. No runtime dependencies: the page is HTML, CSS, a little TypeScript, and pre-recorded clips of the real effect.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2",
    "vite": "^8.1.5"
  }
}
